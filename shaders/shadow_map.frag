
#version 330 core

/*
    Shadow map fragment shader
    Empty shader for depth-only rendering
    Fragment position depth is automatically captured by depth buffer
*/

void main() {
    // No color output needed - depth is captured automatically
}
